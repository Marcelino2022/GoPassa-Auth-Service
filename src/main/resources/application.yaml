server:
  servlet:
    context-path: /
  port: 8080
spring:
  application:
    name: auth_service
  datasource:
    url: jdbc:h2:mem:authService
    driveClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true